"""Central Gemini model identifiers used by MARK L.

Keep the Live API model separate: it has different capabilities and lifecycle
from the standard generate_content helper model.
"""

HELPER_MODEL = "gemini-3.5-flash-lite"
